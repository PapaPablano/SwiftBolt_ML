# Forecasting Lab — minimal deps (no production DB/ML stack)
pandas>=1.5.0
numpy>=1.21.0
PyYAML>=6.0
# For yfinance adapter (optional)
yfinance>=0.2.0

# Optional (Hybrid ensemble + monitoring)
# Kept lightweight: models import these lazily so naive runs still work without them.
xgboost>=2.0.0
statsmodels>=0.14.0
scipy>=1.9.0
