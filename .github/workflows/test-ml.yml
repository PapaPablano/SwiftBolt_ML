name: ML Tests - Legacy (Deprecated, use ml-validation.yml instead)

# DEPRECATED: This workflow is kept for reference only.
# Please use ml-validation.yml for comprehensive ML testing.
#
# This workflow has been superseded by ml-validation.yml which provides:
# - Unit tests with coverage enforcement
# - Code quality & security scanning
# - Integration tests
# - Weekly schedule + manual trigger + dependency change triggers
# - Better artifact management

on:
  workflow_dispatch:  # Manual trigger only - not recommended

jobs:
  deprecation-notice:
    name: ⚠️ Workflow Deprecated
    runs-on: ubuntu-latest

    steps:
    - name: Show deprecation notice
      run: |
        echo "⚠️ WARNING: test-ml.yml is deprecated and should not be used"
        echo ""
        echo "This workflow has been superseded by ml-validation.yml"
        echo ""
        echo "Please use ml-validation.yml for comprehensive ML testing instead."
        echo "It provides:"
        echo "  ✅ Unit tests with coverage enforcement"
        echo "  ✅ Code quality & security scanning"
        echo "  ✅ Integration tests"
        echo "  ✅ Weekly schedule + manual trigger"
        echo "  ✅ Automatic trigger on requirements.txt changes"
        exit 1
